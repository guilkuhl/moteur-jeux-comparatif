# Test fixtures

Images déterministes utilisées par les tests `pytest` — regénérables via
`tests/fixtures/regenerate.py`. Ne pas éditer à la main.

## Contenu

- `inputs/test_small.png` : 32×32, seed fixe, 4 couleurs.
